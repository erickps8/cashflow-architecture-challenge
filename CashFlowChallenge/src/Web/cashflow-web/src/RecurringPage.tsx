import {FormEvent,useEffect,useState} from 'react';import {Plus,Repeat2,X} from 'lucide-react';import * as api from './api';import './forms-modern.css';const money=(v:number)=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});type CreateRequest={id:number;action:string}|null;export default function RecurringPage({createRequest}:{createRequest?:CreateRequest}){const[list,setList]=useState<api.Recurring[]>([]),[accounts,setAccounts]=useState<api.Account[]>([]),[cats,setCats]=useState<api.Category[]>([]),[open,setOpen]=useState(false);const[f,setF]=useState({amount:0,type:2,description:'',accountId:'',categoryId:'',frequency:0,startAt:new Date().toISOString().slice(0,10),endAt:''});const load=()=>Promise.all([api.getRecurring(),api.getAccounts(),api.getCategories()]).then(([a,b,c])=>{setList(a);setAccounts(b);setCats(c)});useEffect(()=>{load()},[]);useEffect(()=>{if(createRequest?.action==='recurring')setOpen(true)},[createRequest?.id]);async function save(e:FormEvent){e.preventDefault();await api.createRecurring({...f,accountId:f.accountId||null,categoryId:f.categoryId||null,startAt:new Date(`${f.startAt}T12:00:00`).toISOString(),endAt:f.endAt?new Date(`${f.endAt}T12:00:00`).toISOString():null});setOpen(false);setF({...f,amount:0,description:'',endAt:''});load()}return <section className="modern-page"><article className="panel modern-list-card"><div className="modern-list-head"><div><span className="section-kicker">RECORRÊNCIAS</span><h2>Compromissos automáticos</h2><p>{list.length} recorrência(s) ativa(s)</p></div><button className="modern-add" onClick={()=>setOpen(true)}><Plus size={18}/>Adicionar</button></div><div className="modern-list">{list.map(x=><div className="modern-row" key={x.id}><div className="modern-row-icon"><Repeat2/></div><div><strong>{x.description}</strong><span>Próxima: {new Date(x.nextOccurrenceAt).toLocaleDateString('pt-BR')}</span></div><strong>{money(x.amount)}</strong></div>)}</div></article>{open&&<div className="modern-modal-backdrop"><section className="modern-modal"><div className="modern-modal-head"><div><span className="section-kicker">NOVA RECORRÊNCIA</span><h2>Adicionar compromisso</h2><p>Cadastre uma vez e deixe o CashFlow projetar os próximos meses.</p></div><button onClick={()=>setOpen(false)}><X/></button></div><form onSubmit={save} className="modern-form"><div className="entry-type-toggle"><button type="button" className={f.type===2?'active expense':''} onClick={()=>setF({...f,type:2,categoryId:''})}>Despesa</button><button type="button" className={f.type===1?'active income':''} onClick={()=>setF({...f,type:1,categoryId:''})}>Receita</button></div><label className="entry-amount">Valor<div><span>R$</span><input type="number" step="0.01" min="0.01" value={f.amount||''} onChange={e=>setF({...f,amount:+e.target.value})} required/></div></label><label>Descrição<input value={f.description} onChange={e=>setF({...f,description:e.target.value})} required/></label><div className="modern-grid"><label>Conta<select value={f.accountId} onChange={e=>setF({...f,accountId:e.target.value})}><option value="">Sem conta</option>{accounts.filter(x=>x.isActive).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Categoria<select value={f.categoryId} onChange={e=>setF({...f,categoryId:e.target.value})}><option value="">Sem categoria</option>{cats.filter(x=>x.type===f.type&&x.isActive).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Começa em<input type="date" value={f.startAt} onChange={e=>setF({...f,startAt:e.target.value})}/></label><label>Termina em <small>opcional</small><input type="date" value={f.endAt} onChange={e=>setF({...f,endAt:e.target.value})}/></label></div><div className="modern-actions"><button type="button" className="modern-cancel" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary-button">Salvar recorrência</button></div></form></section></div>}</section>}