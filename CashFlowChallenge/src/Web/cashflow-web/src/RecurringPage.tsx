import {FormEvent,useEffect,useState} from 'react';import {Pencil,Plus,Repeat2,Trash2,X} from 'lucide-react';import * as api from './api';import './forms-modern.css';const money=(v:number)=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});type CreateRequest={id:number;action:string}|null;const blank=()=>({amount:0,type:2,description:'',accountId:'',categoryId:'',frequency:0,startAt:new Date().toISOString().slice(0,10),endAt:'',isActive:true});export default function RecurringPage({createRequest}:{createRequest?:CreateRequest}){const[list,setList]=useState<api.Recurring[]>([]),[accounts,setAccounts]=useState<api.Account[]>([]),[cats,setCats]=useState<api.Category[]>([]),[open,setOpen]=useState(false),[editing,setEditing]=useState<string|null>(null);const[f,setF]=useState(blank());const load=()=>Promise.all([api.getRecurring(),api.getAccounts(),api.getCategories()]).then(([a,b,c])=>{setList(a);setAccounts(b);setCats(c)});useEffect(()=>{load()},[]);useEffect(()=>{if(createRequest?.action==='recurring'){setEditing(null);setF(blank());setOpen(true)}},[createRequest?.id]);function edit(x:api.Recurring){setEditing(x.id);setF({amount:x.amount,type:x.type,description:x.description,accountId:x.accountId||'',categoryId:x.categoryId||'',frequency:x.frequency,startAt:x.startAt.slice(0,10),endAt:x.endAt?.slice(0,10)||'',isActive:x.isActive});setOpen(true)}async function save(e:FormEvent){e.preventDefault();const body={...f,accountId:f.accountId||null,categoryId:f.categoryId||null,startAt:new Date(`${f.startAt}T12:00:00`).toISOString(),endAt:f.endAt?new Date(`${f.endAt}T12:00:00`).toISOString():null};if(editing)await api.updateRecurring(editing,body);else await api.createRecurring(body);setOpen(false);setEditing(null);setF(blank());load()}async function remove(x:api.Recurring){if(confirm(`Excluir a recorrência “${x.description}”? Os lançamentos já gerados serão mantidos.`)){await api.deleteRecurring(x.id);load()}}return <section className="modern-page"><article className="panel modern-list-card"><div className="modern-list-head"><div><span className="section-kicker">RECORRÊNCIAS</span><h2>Compromissos automáticos</h2><p>{list.filter(x=>x.isActive).length} recorrência(s) ativa(s)</p></div></div><div className="modern-list">{list.map(x=><div className="modern-row" key={x.id}><div className="modern-row-icon"><Repeat2/></div><div><strong>{x.description}</strong><span>{x.isActive?'Próxima: '+new Date(x.nextOccurrenceAt).toLocaleDateString('pt-BR'):'Inativa'}</span></div><strong>{money(x.amount)}</strong><div className="row-actions"><button title="Editar" onClick={()=>edit(x)}><Pencil size={16}/></button><button title="Excluir" onClick={()=>remove(x)}><Trash2 size={16}/></button></div></div>)}</div></article>{open&&<div className="modern-modal-backdrop"><section className="modern-modal"><div className="modern-modal-head"><div><span className="section-kicker">{editing?'EDITAR RECORRÊNCIA':'NOVA RECORRÊNCIA'}</span><h2>{editing?'Editar compromisso':'Adicionar compromisso'}</h2></div><button onClick={()=>setOpen(false)}><X/></button></div><form onSubmit={save} className="modern-form"><div className="entry-type-toggle"><button type="button" className={f.type===2?'active expense':''} onClick={()=>setF({...f,type:2,categoryId:''})}>Despesa</button><button type="button" className={f.type===1?'active income':''} onClick={()=>setF({...f,type:1,categoryId:''})}>Receita</button></div><label className="entry-amount">Valor<div><span>R$</span><input type="number" step="0.01" min="0.01" value={f.amount||''} onChange={e=>setF({...f,amount:+e.target.value})} required/></div></label><label>Descrição<input value={f.description} onChange={e=>setF({...f,description:e.target.value})} required/></label><div className="modern-grid"><label>Conta<select value={f.accountId} onChange={e=>setF({...f,accountId:e.target.value})}><option value="">Sem conta</option>{accounts.filter(x=>x.isActive||x.id===f.accountId).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Categoria<select value={f.categoryId} onChange={e=>setF({...f,categoryId:e.target.value})}><option value="">Sem categoria</option>{cats.filter(x=>x.type===f.type&&(x.isActive||x.id===f.categoryId)).map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Começa em<input type="date" value={f.startAt} onChange={e=>setF({...f,startAt:e.target.value})}/></label><label>Termina em <small>opcional</small><input type="date" value={f.endAt} onChange={e=>setF({...f,endAt:e.target.value})}/></label>{editing&&<label>Status<select value={f.isActive?'1':'0'} onChange={e=>setF({...f,isActive:e.target.value==='1'})}><option value="1">Ativa</option><option value="0">Inativa</option></select></label>}</div><div className="modern-actions"><button type="button" className="modern-cancel" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary-button">Salvar recorrência</button></div></form></section></div>}</section>}
