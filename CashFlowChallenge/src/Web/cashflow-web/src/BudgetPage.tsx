import {FormEvent,useEffect,useState} from 'react';import {PiggyBank,Plus,X} from 'lucide-react';import * as api from './api';import './forms-modern.css';const money=(v:number)=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});type CreateRequest={id:number;action:string}|null;export default function BudgetPage({year,month,createRequest}:{year:number;month:number;createRequest?:CreateRequest}){const[b,sb]=useState<api.Budget|null>(null),[cats,setCats]=useState<api.Category[]>([]),[cat,setCat]=useState(''),[amount,setAmount]=useState(0),[open,setOpen]=useState(false);const load=()=>Promise.all([api.getBudget(year,month).catch(()=>null),api.getCategories()]).then(([x,c])=>{sb(x);setCats(c.filter(y=>y.type===2&&y.isActive))});useEffect(()=>{void load()},[year,month]);useEffect(()=>{if(createRequest?.action==='budget')setOpen(true)},[createRequest?.id]);async function save(e:FormEvent){e.preventDefault();await api.setBudget({year,month,categoryId:cat,plannedAmount:amount});setOpen(false);setCat('');setAmount(0);void load()}return <section className="modern-page"><div className="budget-hero"><div><span>Planejado no mês</span><strong>{money(b?.plannedAmount??0)}</strong><small>Definido no planejamento</small></div><div><span>Realizado</span><strong>{money(b?.actualAmount??0)}</strong><small>O que já virou gasto</small></div><div><span>Disponível no orçamento</span><strong className={(b?.remainingAmount??0)<0?'negative-text':'positive-text'}>{money(b?.remainingAmount??0)}</strong><small>Planejado menos realizado</small></div></div><article className="panel modern-list-card"><div className="modern-list-head"><div><span className="section-kicker">ACOMPANHAMENTO MENSAL</span><h2>Planejado × realizado por categoria</h2><p>O Planejamento Anual define o plano. Aqui você acompanha o mês e ajusta um limite quando precisar.</p></div><button className="modern-add" onClick={()=>setOpen(true)}><Plus size={18}/>Ajustar orçamento</button></div><div className="budget-list">{b?.categories.length?b.categories.map(x=>{const pct=x.plannedAmount>0?Math.min(100,(x.actualAmount/x.plannedAmount)*100):0;return <div className="budget-item" key={x.categoryId}><div><span>{x.categoryName}</span><strong>{money(x.actualAmount)} <small>de {money(x.plannedAmount)}</small></strong></div><div className="budget-progress" aria-label={`${pct.toFixed(0)}% utilizado`}><i style={{width:`${pct}%`}}/></div><small>{x.isOverBudget?`Acima do planejado em ${money(Math.abs(x.remainingAmount))}`:`Disponível: ${money(x.remainingAmount)}`}</small></div>}):<div className="modern-empty"><strong>Nenhum orçamento para este mês</strong><span>Defina o plano no Planejamento Anual ou ajuste uma categoria aqui.</span></div>}</div></article>{open&&<div className="modern-modal-backdrop"><section className="modern-modal compact-modal"><div className="modern-modal-head"><div><span className="section-kicker">AJUSTAR MÊS</span><h2>Orçamento da categoria</h2></div><button onClick={()=>setOpen(false)}><X/></button></div><form onSubmit={save} className="modern-form"><div className="modal-symbol"><PiggyBank/></div><label>Categoria<select value={cat} onChange={e=>setCat(e.target.value)} required><option value="">Escolha uma categoria</option>{cats.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label className="entry-amount">Valor planejado<div><span>R$</span><input type="number" step="0.01" min="0" value={amount||''} onChange={e=>setAmount(+e.target.value)} required/></div></label><div className="modern-actions"><button type="button" className="modern-cancel" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary-button">Salvar orçamento</button></div></form></section></div>}</section>}